{
  "name": "@patron/types",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Shared TypeScript types/contracts consumed across Patron apps.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
