/**
 * @patron/types
 *
 * Shared, framework-agnostic type contracts consumed across the monorepo
 * (API responses, DTO shapes, enums). Business types are added here as
 * features land so the API and admin dashboard stay in sync.
 *
 * Intentionally empty for now — structure only, no business features.
 */

export {};
